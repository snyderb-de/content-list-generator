# Testing Workspace

Use this directory for manual testing support files.

- `testing/sample/` is for local sample inputs used during manual runs.
- `testing/out/` is for local generated outputs from manual runs.
- `testdata/` stays at the repo root for tracked automated test fixtures.
